---
title: 动态规划(状态转移方程)
published: 2023-04-17 21:19:28
tags: [动态规划]
author: 月初
category: 动态规划

---

#### [53. 最大子数组和](https://leetcode.cn/problems/maximum-subarray/)

给你一个整数数组 `nums` ，请你找出一个具有最大和的连续子数组（子数组最少包含一个元素），返回其最大和。

**子数组** 是数组中的一个连续部分。

**示例 1：**

```
输入：nums = [-2,1,-3,4,-1,2,1,-5,4]
输出：6
解释：连续子数组 [4,-1,2,1] 的和最大，为 6 。
```

**示例 2：**

```
输入：nums = [1]
输出：1
```

**示例 3：**

```
输入：nums = [5,4,-1,7,8]
输出：23
```

 

**提示：**

- `1 <= nums.length <= 105`
- `-104 <= nums[i] <= 104`

```c++
int maxSubArray(vector<int>& nums) {
	if(nums.empty())
		return 0;
	vector<int> v(nums);//将vector类型nums全部复制到v中去
	int maxNum = nums[0];
	for(int i = 1; i < nums.size(); i++)
	{
		v[i] = max(v[i - 1] + nums[i], nums[i]);
		if(maxNum < v[i])
			maxNum = v[i];
	}
	return maxNum;
}
```

<mark style="background-color：green">ps:v[ i ]并不是代表的前i + 1 的最大子序列的和。而是包含nuns[i ]的最大子序列的和。</mark>

